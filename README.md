# encodeMeThat - Discord BOT to encode SQLi payloads

Payload encoder using sqlmap tampers. Change **DISCORD-BOT-TOKEN** in code and run!

```python3 encodeMeThat.py```

### Use exemple 
```
!encode MSSQL "' OR 1 -- -"
```
